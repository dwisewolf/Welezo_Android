<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.welezohealth.welezo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".OTP_Activity">
        </activity>
        <activity android:name=".SignUp_Activity"/>
        <activity android:name=".ForgotPassword_Activity"/>

        <activity android:name=".RetypeEmail_Activity" />
        <activity android:name=".NewPassword_Activity"/>
        <activity android:name=".Sign_in_Activity" />
        <activity android:name=".SplashActitivty">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>